name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore GreenBuddy.sln

      - name: Build (Release)
        run: dotnet build GreenBuddy.sln --no-restore -c Release

      - name: Run tests
        run: dotnet test GreenBuddy.sln --no-build --verbosity normal

      # Exempel: använd din secret i ett steg
      - name: Use my secret
        run: |
          echo "Min secret är tillgänglig i denna miljö (värdet maskeras i loggar)."
          echo "Secret length: ${#MY_SECRET}"
        env:
          MY_SECRET: ${{ secrets.YOUR_SECRET_NAME }}

      - name: Publish output
        run: dotnet publish GreenBuddy.sln -c Release -o ./publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: greenbuddy-publish
          path: ./publish

      # 🔔 Discord-notis – körs ALLTID och visar rätt status
      - name: Notify Discord
        if: ${{ always() }}
        env:
          WEBHOOK: ${{ secrets.YOUR_SECRET_NAME }} # Lägg webhook-URL:en i denna secret
          STATUS: ${{ job.status }} # success | failure | cancelled
        run: |
          if [ "$STATUS" = "success" ]; then
            MSG="✅ GreenBuddy CI passerade på branch $GITHUB_REF_NAME
          👉 Run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
          else
            MSG="❌ GreenBuddy CI $STATUS på branch $GITHUB_REF_NAME
          🔎 Run: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
          fi

          # Gör säker JSON (escape:a radbrytningar)
          PAYLOAD=$(printf '{"content":"%s"}' "$(echo "$MSG" | sed ':a;N;$!ba;s/\n/\\n/g')")

          curl -sS -H "Content-Type: application/json" -X POST -d "$PAYLOAD" "$WEBHOOK"
